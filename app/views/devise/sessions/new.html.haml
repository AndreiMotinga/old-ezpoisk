:css
  .footer {
    position: absolute;
    bottom: 0px;
  }

:javascript
  $(document).ready(function(){
    $("#new_session_modal").trigger("click");

    $("#new_session_modal").modal({
        backdrop : "static",
        keyboard: false
    });
  })
