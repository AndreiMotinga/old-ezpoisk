.fixed
  %ul.nav.grey-block
    - SERVICE_CATEGORIES.keys.each do |category|
      - if cp(category: category)
        %li.active= link_to category, services_path(category: category)
      - else
        %li= link_to category, services_path(category: category)

