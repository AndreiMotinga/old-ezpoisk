= title("Услуги: Адвокаты, Медицина, Образование, Ремонтные и др. в США - ezpoisk")
= desc("EZPOISK - Бесплатные обьявления, реклама. Все виды услуг в США. Найти сервис, рекламировать бизнес, получить помощь.")
= render 'services/search_modal'
- content_for :secondary_navbar do
  = render "services/navbar"
- content_for :side do
  = render "shared/side/questions"
- content_for :search do
  .col-sx-12.search
    = link_to "Настроить фильтр", "", class: "btn mobile-btn", "data-target": "#serviceSearch", "data-toggle": :modal, role: :button
    .hidden-xs#search_params= render "shared/search_params"

.col-xs-12
  - @services.each do |service|
    = render 'dashboard/shared/company', record: service

.row
  .col-xs-12
    = render "shared/pagination", records: @services
