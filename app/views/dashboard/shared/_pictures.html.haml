.row
  .col-xs-12
    / Button trigger modal
    -# todo make one button
    .hidden-xs
      %button.btn.btn-primary{"data-target" => "#myModal", "data-toggle" => "modal", :type => "button"}
        Загрузить фотографии

    .visible-xs
      %button.btn.mobile-btn{"data-target" => "#myModal", "data-toggle" => "modal", :type => "button"}
        Загрузить фотографии

    / Modal
    #myModal.modal.fade{"aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
      .modal-dialog{:role => "document"}
        .modal-content
          .modal-body
            = form_for([:dashboard, Picture.new], html: { multipart: true, class: "dropzone"}) do |f|
              = f.hidden_field(:imageable_id, value: record.id)
              = f.hidden_field(:imageable_type, value: record.class.to_s)
              .fallback
                = f.file_field :image
                %br/
                = f.submit "Upload"
          .modal-footer
            %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close
    / Modal

/ Pictures
%br/
.row
  .col-xs-12{"data-pictures": ""}
    = render record.pictures
    %br/
/ Pictures
