= title("|Агенства по трудоустройству")
= desc("eZpoisk - Агентство по трудоустройству #{@job_agency.title}, #{@job_agency.address}")
- content_for :secondary_navbar do
  = render "jobs/navbar"

= render "shared/show",
          record: @job_agency,
          template: 'dashboard/shared/company'
