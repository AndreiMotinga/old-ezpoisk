= title("Продажа, Автомобили, Электроника, Одежда, Магазины, Распродажи в США - ezpoisk")
= desc("EZPOISK - Обьявления о продаже в США: Нью Йорке, Сан-Франциско, Лос-Анджелес, Хьюстон, Майами и др.")
- content_for :secondary_navbar do
  = render "sales/navbar"
= render 'sales/search_modal'
= content_for :top_partner do
  = render "sales/partner"

.col-md-8
  .row
    .visible-xs.col-xs-3= link_to '<', root_path, class: "mobile-btn mobile-back-btn"
    = render 'sales/search_modal_trigger'
    %br/
    .hidden-xs= render "shared/search_params"

  %br/

  .row
    .col-xs-12
      - @sales.each do |sale|
        = render 'dashboard/sales/sale', record: sale

    .col-xs-12
      = render "shared/pagination", records: @sales
