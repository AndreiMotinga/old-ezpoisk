$("[data-id='listings']").append("<%= j render(@records) %>");
<% if @records.next_page %>
  $(".pagination").replaceWith("<%= j paginate(@records, :theme => 'twitter-bootstrap-3') %>")
<% else  %>
  $(".pagination").remove();
<% end %>
