= title("#{@job.title} #{@job.city.name} #{@job.state.name} - работа '#{@job.post_type}'")
= desc("ezpoisk - Требуется на работу #{@job.title} - #{@job.category}")
- content_for :secondary_navbar do
  = render "ezjob/navbar"

= render 'shared/show/company', record: @job
