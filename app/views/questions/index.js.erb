$("[data-id='listings']").append("<%= j render(@questions) %>");
<% if @questions.next_page %>
  $(".pagination").replaceWith("<%= j paginate(@questions, :theme => 'twitter-bootstrap-3') %>")
<% else  %>
  $(".pagination").remove();
<% end %>
