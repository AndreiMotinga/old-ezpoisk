$("[data-id='listings']").append("<%= j render(@questions) %>");
<% if @questions.next_page %>
  $(".pagination").replaceWith("<%= j paginate(@questions) %>")
<% else  %>
  $(".pagination").remove();
<% end %>
