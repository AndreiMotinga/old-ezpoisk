- if current_user.try(:voted_for?, question)
  = voted_btn(question)
- else
  = upvote_btn(question)
= link_to "Downvote", downvote_question_path(question), method: :put, remote: true,
  class: "text text-muted downvote"
