$("[data-id='listings']").append("<%= j render(@entries) %>");
<% if @entries.next_page %>
  $(".pagination").replaceWith("<%= j paginate(@entries, :theme => 'twitter-bootstrap-3') %>")
<% else  %>
  $(".pagination").remove();
<% end %>
