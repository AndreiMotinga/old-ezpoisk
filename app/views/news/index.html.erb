<div class="row news">
<div class="col-lg-8 col-lg-offset-2  col-md-10 col-md-offset-1">


  <% @posts.each do |post| %>
    <%= render 'news/posts', post: post %>
  <% end %>

  <div class="text-center">
    <%= paginate @posts, :theme => 'twitter-bootstrap-3' %></span>
  </div>

</div>
</div>
