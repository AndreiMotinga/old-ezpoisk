- content_for :tab_links do
  = render 'layouts/tab_links/real_estate'

/ Main
.row
  %h4.pull-right
    %a= "Всего #{@re_agencies.total_count}"
  = render 'real_estate/re_agencies/search_modal'

.row
  %hr/
  - @re_agencies.each do |re_agency|
    = render 'dashboard/re_agencies/re_agency', record: re_agency

= render "shared/pagination", records: @re_agencies
/ Main
