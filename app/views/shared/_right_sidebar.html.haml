- Post.important.desc.limit(6).each do |record|
  %div.side-post
    = link_to "https://#{record.link}", target: "blank" do
      = image_tag record.logo, class: "img-responsive"
      .text= record.title
