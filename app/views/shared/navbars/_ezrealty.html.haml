/ Desktop
.hidden-xs
  %nav.navbar.secondary-navbar.white
    .container-fluid
      / Brand and toggle get grouped for better mobile display
      .navbar-header
        %button.navbar-toggle.collapsed{ "aria-expanded" => "false",
                                         "data-target" => "#dashboard-nav-bar",
                                         "data-toggle" => "collapse",
                                         :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
      / Collect the nav links, forms, and other content for toggling
      #dashboard-nav-bar.collapse.navbar-collapse
        %ul.nav.navbar-nav.navbar-left
          %li{:class => (cp(re_privates_path))}
            = link_to "Частная", re_privates_path
          %li{:class => (cp(re_commercials_path))}
            = link_to "Коммерческая", re_commercials_path
          %li{:class => (cp(re_agencies_path))}
            = link_to "Агентства", re_agencies_path
          -# %li{:class => (cp(re_finances_path))}
          -#   = link_to "Финансирование", re_finances_path
      / /.navbar-collapse
    / /.container-fluid
  / /nav
