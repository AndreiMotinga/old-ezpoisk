.hidden-xs
  %nav.navbar.secondary-navbar.white
    .container-fluid
      / Brand and toggle get grouped for better mobile display
      .navbar-header
        %button.navbar-toggle.collapsed{ "aria-expanded" => "false",
                                         "data-target" => "#dashboard-nav-bar",
                                         "data-toggle" => "collapse",
                                         :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
      / Collect the nav links, forms, and other content for toggling
      #dashboard-nav-bar.collapse.navbar-collapse
        %ul.nav.navbar-nav.navbar-left
          %li{:class => ("active" if cp(jobs_path))}
            = link_to "Работа", jobs_path
          %li{:class => ("active" if cp(job_agencies_path))}
            = link_to "Агентства", job_agencies_path
      / /.navbar-collapse
    / /.container-fluid
