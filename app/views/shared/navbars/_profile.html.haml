/ Desktop
.hidden-xs
  %nav.navbar.secondary-navbar.white
    .container-fluid
      / Brand and toggle get grouped for better mobile display
      .navbar-header
        %button.navbar-toggle.collapsed{ "aria-expanded" => "false",
                                         "data-target" => "#dashboard-nav-bar",
                                         "data-toggle" => "collapse",
                                         :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
      / Collect the nav links, forms, and other content for toggling
      #dashboard-nav-bar.collapse.navbar-collapse
        %ul.nav.navbar-nav.navbar-left
          %li= link_to "Мои объявления", dashboard_path
          %li{class: cp(dashboard_posts_path)}
            = link_to "Мои новости", dashboard_posts_path
          %li{class: cp(dashboard_favorites_path)}
            = link_to "Избранное", dashboard_favorites_path
        %ul.nav.navbar-nav.navbar-right
          -# reklama
          - if current_user.try(:admin)
            %li{class: cp(dashboard_partners_path)}
              = link_to "Реклама", dashboard_partners_path,  "data-toggle": "modal"
          %li{class: cp(edit_user_registration_path)}
            = link_to "Личные данные", edit_dashboard_profile_path(current_user)
      / /.navbar-collapse
    / /.container-fluid
  / /nav
