.row.post-item.white-block{ class: ("user-hidden" if record.user_hidden?(current_user)), "data-hidden": record.id }
  .col-xs-4.col-sm-4.logo-js
    = link_to link_to_show_page(record) do
      = image_tag record.logo.url(:medium), alt: record.title, class: "img-responsive"
  .col-xs-8.col-sm-8
    / line 1
    .hidden-xs
      - if home_controller?
        %h5.title= link_to record.title, link_to_show_page(record)
      - else
        %h4.title= link_to record.title, link_to_show_page(record)
    .visible-xs
      %h5.title= link_to record.title, link_to_show_page(record)

    /line 2
    .hidden-xs
      - if record.street.present? && !home_controller?
        %h5= record.street
        %hr.post-devider/

    /line 3
    - if record.try(:short_description).present?
      .hidden-xs
        %p.text-muted= record.short_description
        %hr.post-devider/

    /line 4
    %span.glyphicon.glyphicon-earphone
    %span.company-phone= record.phone
    %hr.post-devider/

    / line 6
    .hidden-xs
      - if record.try(:site).present?
        %strong= link_to record.site, "http://#{record.site}", target: "_blank"

    / line 7
    .pull-right= render "shared/favor_links", record: record
    %span= time_ago_in_words record.updated_at
