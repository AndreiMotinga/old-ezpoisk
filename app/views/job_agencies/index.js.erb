$("[data-id='listings']").append("<%= j render(@job_agencies) %>");
<% if @job_agencies.next_page %>
  $(".pagination").replaceWith("<%= j paginate(@job_agencies, :theme => 'twitter-bootstrap-3') %>")
<% else  %>
  $(".pagination").remove();
<% end %>
